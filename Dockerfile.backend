FROM node:18-alpine
WORKDIR /app

# Copy root package files and install dependencies
COPY package.json ./
RUN npm install --production

# Copy backend application code
COPY backend ./backend

# Alternatively, move code to root for simplicity
WORKDIR /app/backend
EXPOSE 3000

CMD ["node", "index.js"]
