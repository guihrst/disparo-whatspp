version: "3.7"
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.backend
    env_file:
      - backend/.env
    ports:
      - "3000:3000"
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.frontend
    ports:
      - "8080:80"
