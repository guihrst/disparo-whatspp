version: "3.7"
services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile.backend
    env_file:
      - backend/.env
    ports:
      - "3000:3000"
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile.frontend
    ports:
      - "8080:80"
